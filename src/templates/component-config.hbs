import { {{name}} } from "@nulogy/components";
import { PropTypes } from 'react-view';

export const {{toCamelCase name}}Config = {
  componentName: "{{name}}",
  scope: {
    {{name}}
  },
  props: {
    children: {
      value: "Hello",
      type: PropTypes.ReactNode,
      description: "Visible message."
    },
  }
}