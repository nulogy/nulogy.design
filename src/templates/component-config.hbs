import {
{{name}}
} from "@nulogy/components"; import { PropTypes } from 'react-view'; export
const config = { componentName: "{{name}}", scope: {
{{name}}
}, props: { children: { value: "Hello", type: PropTypes.ReactNode, description:
"Visible message." }, } }