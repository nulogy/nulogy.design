import * as React from "react"
import {Page} from "@nulogy/components";

const IndexPage = ({location}) => {
  return (
    <Page title="Patterns" location={location}>

    </Page>
  )
}

export default IndexPage
