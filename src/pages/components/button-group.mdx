import { ButtonGroup } from "@nulogy/components";
import { config } from "../../playgrounds/button-group";

# ButtonGroup

Button Groups associate a set of related buttons together.

<Playground {...config} />

## Use when

Always use the Button Group component when there are multiple buttons beside each other.

## Props

<PropsTable propsConfig={config.props} />
