import { Tabs, Tab } from "@nulogy/components";
import { config } from "../../playgrounds/tabs";

# Tabs

Tabs is a component.

<Playground {...config} />

### Overflow

When the width of the individual Tab components exceed the total width of the Tabs component, scrolling becomes enabled and buttons are placed on the edges to make it easier to scroll to off-screen Tabs on mobile devices.

<Example componentName="tabs" scope={{ Tabs, Tab }} example="Overflow" />

## Props

<PropsTable propsConfig={config.props} />

## Related Components

- [Box](/box)
