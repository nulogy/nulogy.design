import { Link } from "@nulogy/components";
import { config } from "../../playgrounds/link";

# Link

A styled `a` tag that can be used to send users to a URL.

<Playground {...config} />

## Props

<PropsTable propsConfig={config.props} />
