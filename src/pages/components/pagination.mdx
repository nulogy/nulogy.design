import { Pagination } from "@nulogy/components";
import { config } from "../../playgrounds/pagination";

# Pagination

Pagination is used for splitting data up into multiple pages.

<Playground {...config} />

## Props

<PropsTable propsConfig={config.props} />
