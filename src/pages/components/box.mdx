import { Box } from "@nulogy/components";
import { config } from "../../playgrounds/box";

# Box

A layout component for controlling width, margin, padding, colour and shadows.

<Playground {...config} />

## Props

<PropsTable propsConfig={config.props} />

The Box component has access to `space`, `colour`, `layout`, `border`, `boxShadow`, `textAlign`, and `position` style props. See the [style prop](/guides/style-props) documentation for a full list of available props.
