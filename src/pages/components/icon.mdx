import { Icon, Link, Box, Text, Flex } from "@nulogy/components";
import { config } from "../../playgrounds/icon";
import iconList from "../../utils/iconList";
import { SLACK_LINK, CODE_WIDTH } from "../../components/CONSTANTS";

# Icon

Icons can be used alongside text to help assist users in finding certain actions on a page.

<Playground {...config} />

If your interface requires an icon not listed here, please post a message in the <Link fontSize="20px" href={SLACK_LINK}>#design-ops</Link> slack channel.

## Available Icons

Nulogy uses a selection of solid style <Link fontSize="20px" href="https://fonts.google.com/icons">Material Design</Link> icons.

<Flex flexWrap="wrap" width="100%" maxWidth={CODE_WIDTH} mx="auto">
  {iconList.map((icon) => (
    <Flex width="33%" p="x1">
      <Icon icon={icon} size="x3" pr="x1" />
      <Text>{icon}</Text>
    </Flex>
  ))}
</Flex>

## Props

<PropsTable propsConfig={config.props} />

## Related Components

- [IconicButton](/iconic-button)
