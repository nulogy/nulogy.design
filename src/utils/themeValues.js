import { theme } from "@nulogy/components"

export const COLOURS = Object.keys(theme.colors)
export const SIZES = Object.keys(theme.space)
export const SHADOWS = Object.keys(theme.shadows)
export const RADII = Object.keys(theme.radii)
